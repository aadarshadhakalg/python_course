[project]
name = "myblog"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django>=5.1.7",
    "django-stubs>=5.1.3",
    "pillow>=11.1.0",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
typeCheckingMode = "off"
